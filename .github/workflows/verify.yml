---
name: Verify
on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
# holden:ignore:HLD_CI_017:by Design
  verify:
    uses: JamesWoolfenden/.github/.github/workflows/terraform-verify.yml@main
